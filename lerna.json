{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "version": {
      "allowBranch": ["main"],
      "conventionalCommits": true,
      "ignoreChanges": ["**/*.md"],
      "message": "chore: publish a new version"
    }
  }
}
