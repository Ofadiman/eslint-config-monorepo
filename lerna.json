{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": false,
  "command": {
    "publish": {
      "allowBranch": ["main"]
    },
    "version": {
      "allowBranch": ["main"],
      "ignoreChanges": ["**/*.test.ts", "**/*.test.utils.ts", "**/*.md"],
      "message": "chore: new release"
    }
  }
}
